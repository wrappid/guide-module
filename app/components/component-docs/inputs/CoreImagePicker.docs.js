import { CoreImagePicker } from "@wrappid/core";
  
import ComponentDocs from "../ComponentDocs";

export default function CoreImagePickerDocs() {
  return (
    <ComponentDocs
      component={CoreImagePicker}
      description="The CoreImagePicker helps to directly upload Image."
    />
  );
}